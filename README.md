git Bash

git config --list  // Провекра акаунта

git branch vet1    // Создание ветки

git switch vet1    // переход на ветку

git push --set-upstream origin vet1  // пушим

Командная строка

git status

git add .

git commit -m "Test"

git push --set-upstream origin vet1

git remote set-url origin https://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@github.com/ds3426484-sys/proekt.git

git branch

git branch main

git merge vet1

git push --set-upstream origin vet1

